import { InvestmentvalPipe } from './investmentval.pipe';

describe('InvestmentvalPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentvalPipe();
    expect(pipe).toBeTruthy();
  });
});
