import { CurrentvalPipe } from './currentval.pipe';

describe('CurrentvalPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentvalPipe();
    expect(pipe).toBeTruthy();
  });
});
